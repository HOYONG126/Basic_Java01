package basic;
//자바는 전역변수 없다.
public class LocalVariableApp {
	
	int number=100;  //멤버변수
	
	public static void main(String[] args) {
		//지역변수(local Variable) : 특정 구문에서 선언된 변수 - 블럭({}) 내부에서 선언
		//지역변수는 선언된 구문에서만 사용가능
		//지역변수가 선언된 구문이 종료되면 지역변수 자동소멸
		//괄호안 변수는 괄호 안에서만 사용가능하다.
	}

}
